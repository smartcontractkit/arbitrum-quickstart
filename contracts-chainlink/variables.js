

module.exports = {
    // The ETH_URL here must be an HTTP(S) url.  For example, if you're using INFURA,
    // it will look something like https://:<secret key>@rinkeby.infura.io/v3/<project ID>
    ETH_URL: '',
    MNEMONIC_OR_PRIVATE_KEY: '',
    JOB_ID: '',
    CHAINLINK_NODE_ADDR: '',
    LINK_PAYMENT: '1000000000000000000',
}

